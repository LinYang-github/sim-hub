[project]
name = "simhub-sdk"
version = "0.1.0"
description = "SimHub SDK for Python"
dependencies = [
    "requests>=2.25.0",
]

[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[tool.pytest.ini_options]
testpaths = ["tests"]
